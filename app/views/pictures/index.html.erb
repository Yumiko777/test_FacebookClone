<div class='container'>
<h1 class='title' >写真の一覧</h1>
<p><%= notice %></p>
<p><%= link_to '写真新規作成画面へ' , new_picture_path =%></p>
<table>
  <tr>
    <th>内容</th>
  </tr>
<% @pictures.each do |picture| %>
  <tr>
    <td><%= picture.content %></td>
    <td><%= picture.user.name %></td>
    <td><%= link_to '詳細を確認する', picture_path(picture.id) %></td>
    <td><%= link_to '写真を編集する', edit_picture_path(picture.id), data: { confirm: '本当に編集していいですか？' }%></td>
    <td><%= link_to '写真を削除する', picture_path(picture.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %></td>
  </tr>
<% end %>
</table>
<br>
<%= link_to '新しい写真を投稿する', new_picture_path =%>
